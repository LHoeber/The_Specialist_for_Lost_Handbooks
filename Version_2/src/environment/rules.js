/**
 * Game rules: module behaviors, interactions, breakage, etc. Direct port of
 * Version_1/src/environment/rules.py -- empty placeholder here too, for now.
 * No module has real behavior yet beyond rendering, room navigation, and the
 * Workbench's cabinet door. Rules get added here as each device's mechanics
 * are designed (see ../Version_1_web/CLAUDE.md, "Going forward").
 */
window.Game = window.Game || {};

Game.Rules = {};
