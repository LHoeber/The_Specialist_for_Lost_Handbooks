# src/

The rebuilt game code. `../../Version_0/game/` and `../../Version_0/rendering/` show what the previous prototype's structure and module state machines looked like — useful as prior art, not as a template to copy. The architecture here should be designed fresh for coherence (see `../CLAUDE.md` for the requirements it needs to satisfy) rather than inheriting Version_0's structure by default.
